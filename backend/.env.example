DATABASE_URL=postgresql+asyncpg://grudge:grudge_dev_password@localhost:5432/grudge_dev
OAUTH_STATE_SECRET=dev-only-change-me
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FRONTEND_BASE_URL=http://localhost:5173
BACKEND_BASE_URL=http://localhost:8000
DISCORD_WEBHOOK_URL=  # watchdog stuck-job alerts - empty = alerts silently skipped, see TODO.md
# SANDBOX_BACKEND=nsjail  # production MUST set this - defaults to "dev" (insecure, no OS isolation) for local dev, see worker.py
